import { forwardPage } from '../../../helpers/forward-page';
import { module, test } from 'qunit';

module('Unit | Helper | forward page');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = forwardPage(42);
  assert.ok(result);
});
