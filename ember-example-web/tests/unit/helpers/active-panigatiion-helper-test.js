import { activePanigatiionHelper } from '../../../helpers/active-panigatiion-helper';
import { module, test } from 'qunit';

module('Unit | Helper | active panigatiion helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = activePanigatiionHelper(42);
  assert.ok(result);
});
