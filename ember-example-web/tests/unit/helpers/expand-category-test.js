import { expandCategory } from '../../../helpers/expand-category';
import { module, test } from 'qunit';

module('Unit | Helper | expand category');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = expandCategory(42);
  assert.ok(result);
});
