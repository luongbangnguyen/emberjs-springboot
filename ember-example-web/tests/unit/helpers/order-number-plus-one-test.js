import { orderNumberPlusOne } from '../../../helpers/order-number-plus-one';
import { module, test } from 'qunit';

module('Unit | Helper | order number plus one');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = orderNumberPlusOne(42);
  assert.ok(result);
});
