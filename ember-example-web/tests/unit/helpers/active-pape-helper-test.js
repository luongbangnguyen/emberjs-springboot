import { activePapeHelper } from '../../../helpers/active-pape-helper';
import { module, test } from 'qunit';

module('Unit | Helper | active pape helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = activePapeHelper(42);
  assert.ok(result);
});
