import { arrayNumberHelper } from '../../../helpers/array-number-helper';
import { module, test } from 'qunit';

module('Unit | Helper | array number helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = arrayNumberHelper(42);
  assert.ok(result);
});
