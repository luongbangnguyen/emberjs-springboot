import { orderNumberFlowPage } from '../../../helpers/order-number-flow-page';
import { module, test } from 'qunit';

module('Unit | Helper | order number flow page');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = orderNumberFlowPage(42);
  assert.ok(result);
});
