import { determineCategoryChosen } from '../../../helpers/determine-category-chosen';
import { module, test } from 'qunit';

module('Unit | Helper | determine category chosen');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = determineCategoryChosen(42);
  assert.ok(result);
});
